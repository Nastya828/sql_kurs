1. Employees и Territories - многие-ко-многим - дополнительная таблица EmployeeTerritories;

2. Orders и Products - многие-ко-многим - дополнительная таблица Order Details;

3. Orders к Customers - один-к-одному(один заказ к одному покупателю) - поле в таблице Orders;

4. Employees к Orders - один-ко-многим(один заказ к нескольким работникам) -  поле в таблице Orders;

5. Region к Territories - один-ко-многим(один регион ко многим территориям) - поле в таблице Territories;

6. Categories к Products -  один-ко-многим(одна категория ко многим продуктам) - поле в таблице Products;

7. Suppliers к Products -  один-ко-многим(один грузоотправитель ко многим продуктам) - поле в таблице Products;

8. Customers к CustomerTypes - многие-ко-многим - дополнительная таблица CustomerCustomerDemo;

9. ReportsTo к Employees - один-ко-многим(один отчет к нескольким работникам) -  поле в таблице Employees;
